/**
 * modal.js
 * No popups/modals exist on the home page today. This module is kept as an
 * empty, self-contained placeholder so the file structure matches other
 * Going2Pass templates and future modals (e.g. a promo popup) can be added
 * here without touching unrelated modules.
 */
window.Going2Pass = window.Going2Pass || {};

window.Going2Pass.modal = (function () {
  'use strict';

  function init() {
    // Intentionally empty — no modal markup on this page yet.
  }

  return { init: init };
})();
